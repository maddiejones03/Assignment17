# For score_fraud_batch.py (match your notebook / sklearn pipeline)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# HTTP scorer (python/scorer_api.py) — omit if you only use CLI batch script
fastapi>=0.109
uvicorn[standard]>=0.27
pydantic>=2.0
